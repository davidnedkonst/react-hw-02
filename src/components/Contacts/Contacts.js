import Section from "../Section";

export default function Contacts() {
    return (
        <div>
            <Section title='Contacts'>
                Contacts
            </Section>
        </div>
    );
}